# Generated by scripts/generate-shell-completions.py; do not edit.

_ghostty_qt_reply_words()
{
  local words=$1 value=$2 candidate
  while IFS= read -r candidate; do
    [[ $candidate == *= ]] && COMPREPLY+=("$candidate") || COMPREPLY+=("$candidate " )
  done < <(compgen -W "$words" -- "$value")
}

_ghostty_qt_complete_value()
{
  local action=$1 option=$2 value=$3 reply_prefix=$4 candidate
  case "$action:$option" in
  :--working-directory)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'home inherit' -- "$value")
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate/")
    done < <(compgen -d -- "$value")
    ;;
  :--title)
    return 0
    ;;
  :--font-family)
    while IFS= read -r candidate; do
      [[ $candidate == "$value"* ]] || continue
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(command ghostty-qt +list-fonts 2>/dev/null | grep '^[A-Z]')
    ;;
  :--font-size)
    return 0
    ;;
  :--class)
    return 0
    ;;
  :--config-default-files)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'true false' -- "$value")
    ;;
  :--scrollback-lines)
    return 0
    ;;
  :--wait-after-command)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'true false' -- "$value")
    ;;
  :--single-instance)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'false true detect' -- "$value")
    ;;
  :--initial-window)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'true false' -- "$value")
    ;;
  +new-window:--class)
    return 0
    ;;
  +new-window:--working-directory)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'home inherit' -- "$value")
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate/")
    done < <(compgen -d -- "$value")
    ;;
  +new-window:--command)
    return 0
    ;;
  +new-window:--shell-integration)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'none detect bash elvish fish nushell zsh' -- "$value")
    ;;
  +new-window:--title)
    return 0
    ;;
  +new-tab:--class)
    return 0
    ;;
  +new-tab:--surface-id)
    return 0
    ;;
  +new-tab:--working-directory)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'home inherit' -- "$value")
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate/")
    done < <(compgen -d -- "$value")
    ;;
  +new-tab:--command)
    return 0
    ;;
  +new-tab:--shell-integration)
    while IFS= read -r candidate; do
      COMPREPLY+=("$reply_prefix$candidate " )
    done < <(compgen -W 'none detect bash elvish fish nushell zsh' -- "$value")
    ;;
  +new-tab:--title)
    return 0
    ;;
  +toggle-quick-terminal:--class)
    return 0
    ;;
  *) return 1 ;;
  esac
  return 0
}

_ghostty_qt()
{
  local cur prev action='' boundary=-1 option='' value='' reply_prefix=''
  local i word candidate
  COMPREPLY=()
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=''
  (( COMP_CWORD > 0 )) && prev=${COMP_WORDS[COMP_CWORD-1]}

  for ((i = 1; i < COMP_CWORD; ++i)); do
    word=${COMP_WORDS[i]}
    if [[ $word == -e || $word == -- ]]; then
      boundary=$i
      break
    fi
    [[ $word == +* ]] && action=$word
  done

  if (( boundary >= 0 )); then
    if (( COMP_CWORD == boundary + 1 )); then
      while IFS= read -r candidate; do COMPREPLY+=("$candidate " ); done < <(compgen -c -- "$cur")
    else
      compopt -o filenames 2>/dev/null || true
      mapfile -t COMPREPLY < <(compgen -f -- "$cur")
    fi
    return 0
  fi

  if [[ $cur == --*=* ]]; then
    option=${cur%%=*}
    value=${cur#*=}
    reply_prefix=$option=
    _ghostty_qt_complete_value "$action" "$option" "$value" "$reply_prefix"
    return 0
  fi
  if [[ $cur == '=' ]]; then
    option=$prev
    _ghostty_qt_complete_value "$action" "$option" '' ''
    return 0
  fi
  if [[ $prev == '=' && $COMP_CWORD -gt 1 ]]; then
    option=${COMP_WORDS[COMP_CWORD-2]}
    _ghostty_qt_complete_value "$action" "$option" "$cur" ''
    return 0
  fi

  if [[ -z $action ]]; then
    case $prev in
    --working-directory | --title | --font-family | --font-size | --class | --config-default-files | --scrollback-lines | --single-instance | --initial-window)
      _ghostty_qt_complete_value '' "$prev" "$cur" ''
      return 0
      ;;
    esac
  fi

  if [[ -n $action ]]; then
    case $action in
    +new-window)
      _ghostty_qt_reply_words '--class= --working-directory= --command= --shell-integration= --title= -e --help' "$cur"
      return 0
      ;;
    +new-tab)
      _ghostty_qt_reply_words '--class= --surface-id= --working-directory= --command= --shell-integration= --title= -e --help' "$cur"
      return 0
      ;;
    +toggle-quick-terminal)
      _ghostty_qt_reply_words '--class= --help' "$cur"
      return 0
      ;;
    esac
  fi

  _ghostty_qt_reply_words '-h --help -v --version --working-directory= --title= --font-family= --font-size= --class= --config-default-files= --scrollback-lines= --hold --wait-after-command --single-instance= --initial-window= -e -- +new-window +new-tab +toggle-quick-terminal' "$cur"
}

complete -o nospace -o bashdefault -F _ghostty_qt ghostty-qt
