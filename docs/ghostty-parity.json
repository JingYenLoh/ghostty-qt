{
  "schema_version": 2,
  "upstream": {
    "project": "ghostty",
    "revision_file": "GHOSTTY_REVISION",
    "source_directory": "ghostty"
  },
  "scope": {
    "host_os": [
      "linux"
    ],
    "display_servers": [
      "wayland"
    ],
    "toolkit": "qt-quick",
    "shared_config": {
      "enabled": true,
      "format": "ghostty",
      "policy": "Portable and Linux Ghostty keys retain upstream names and semantics."
    },
    "toolkit_mapping_policy": "GTK-prefixed settings remain GTK-only; meaningful Qt equivalents use independently designed ghostty-qt frontend settings.",
    "excluded_platforms": [
      "freebsd",
      "ios",
      "macos",
      "x11"
    ]
  },
  "status_definitions": {
    "supported": "Upstream-compatible behavior is implemented and tested.",
    "partial": "Some behavior exists, but the upstream interface or semantics are incomplete.",
    "planned": "The item is in scope but is not implemented yet.",
    "blocked_upstream": "Implementation requires a missing stable libghostty-vt capability.",
    "not_applicable": "The item is outside the Linux/Wayland/Qt parity target."
  },
  "scope_definitions": {
    "portable": "Platform-neutral Ghostty behavior.",
    "linux": "Linux behavior independent of the display server.",
    "wayland": "Linux Wayland behavior.",
    "qt_mapping": "GTK-facing behavior with a meaningful Qt equivalent.",
    "gtk_only": "GTK implementation detail without a Qt parity requirement.",
    "macos": "macOS-only behavior.",
    "x11": "X11-only behavior."
  },
  "inventories": {
    "config_keys": {
      "source": "src/config/Config.zig",
      "default": {
        "status": "planned",
        "scope": "portable"
      },
      "entries": [
        "abnormal-command-exit-runtime",
        "adjust-box-thickness",
        "adjust-cell-height",
        "adjust-cell-width",
        "adjust-cursor-height",
        "adjust-cursor-thickness",
        "adjust-font-baseline",
        "adjust-icon-height",
        "adjust-overline-position",
        "adjust-overline-thickness",
        "adjust-strikethrough-position",
        "adjust-strikethrough-thickness",
        "adjust-underline-position",
        "adjust-underline-thickness",
        "alpha-blending",
        "app-notifications",
        "async-backend",
        "auto-update",
        "auto-update-channel",
        "background",
        "background-blur",
        "background-image",
        "background-image-fit",
        "background-image-opacity",
        "background-image-position",
        "background-image-repeat",
        "background-opacity",
        "background-opacity-cells",
        "bell-audio-path",
        "bell-audio-volume",
        "bell-features",
        "bold-color",
        "class",
        "click-repeat-interval",
        "clipboard-codepoint-map",
        "clipboard-paste-bracketed-safe",
        "clipboard-paste-protection",
        "clipboard-read",
        "clipboard-trim-trailing-spaces",
        "clipboard-write",
        "command",
        "command-palette-entry",
        "config-default-files",
        "config-file",
        "confirm-close-surface",
        "copy-on-select",
        "cursor-click-to-move",
        "cursor-color",
        "cursor-opacity",
        "cursor-style",
        "cursor-style-blink",
        "cursor-text",
        "custom-shader",
        "custom-shader-animation",
        "desktop-notifications",
        "enquiry-response",
        "env",
        "faint-opacity",
        "focus-follows-mouse",
        "font-codepoint-map",
        "font-family",
        "font-family-bold",
        "font-family-bold-italic",
        "font-family-italic",
        "font-feature",
        "font-shaping-break",
        "font-size",
        "font-style",
        "font-style-bold",
        "font-style-bold-italic",
        "font-style-italic",
        "font-synthetic-style",
        "font-thicken",
        "font-thicken-strength",
        "font-variation",
        "font-variation-bold",
        "font-variation-bold-italic",
        "font-variation-italic",
        "foreground",
        "freetype-load-flags",
        "fullscreen",
        "grapheme-width-method",
        "gtk-custom-css",
        "gtk-horizontal-tab-scroll",
        "gtk-opengl-debug",
        "gtk-quick-terminal-layer",
        "gtk-quick-terminal-namespace",
        "gtk-single-instance",
        "gtk-tabs-location",
        "gtk-titlebar",
        "gtk-titlebar-hide-when-maximized",
        "gtk-titlebar-style",
        "gtk-toolbar-style",
        "gtk-wide-tabs",
        "image-storage-limit",
        "initial-command",
        "initial-window",
        "input",
        "key-remap",
        "keybind",
        "language",
        "link",
        "link-previews",
        "link-url",
        "linux-cgroup",
        "linux-cgroup-hard-fail",
        "linux-cgroup-memory-limit",
        "linux-cgroup-processes-limit",
        "macos-applescript",
        "macos-auto-secure-input",
        "macos-custom-icon",
        "macos-dock-drop-behavior",
        "macos-hidden",
        "macos-icon",
        "macos-icon-frame",
        "macos-icon-ghost-color",
        "macos-icon-screen-color",
        "macos-non-native-fullscreen",
        "macos-option-as-alt",
        "macos-secure-input-indication",
        "macos-shortcuts",
        "macos-titlebar-proxy-icon",
        "macos-titlebar-style",
        "macos-window-buttons",
        "macos-window-shadow",
        "maximize",
        "middle-click-action",
        "minimum-contrast",
        "mouse-hide-while-typing",
        "mouse-reporting",
        "mouse-scroll-multiplier",
        "mouse-shift-capture",
        "notify-on-command-finish",
        "notify-on-command-finish-action",
        "notify-on-command-finish-after",
        "osc-color-report-format",
        "palette",
        "palette-generate",
        "palette-harmonious",
        "progress-style",
        "quick-terminal-animation-duration",
        "quick-terminal-autohide",
        "quick-terminal-keyboard-interactivity",
        "quick-terminal-position",
        "quick-terminal-screen",
        "quick-terminal-size",
        "quick-terminal-space-behavior",
        "quit-after-last-window-closed",
        "quit-after-last-window-closed-delay",
        "resize-overlay",
        "resize-overlay-duration",
        "resize-overlay-position",
        "right-click-action",
        "scroll-to-bottom",
        "scrollback-compression",
        "scrollback-limit",
        "scrollbar",
        "search-background",
        "search-foreground",
        "search-selected-background",
        "search-selected-foreground",
        "selection-background",
        "selection-clear-on-copy",
        "selection-clear-on-typing",
        "selection-foreground",
        "selection-word-chars",
        "shell-integration",
        "shell-integration-features",
        "split-divider-color",
        "split-inherit-working-directory",
        "split-preserve-zoom",
        "tab-inherit-working-directory",
        "term",
        "theme",
        "title",
        "title-report",
        "undo-timeout",
        "unfocused-split-fill",
        "unfocused-split-opacity",
        "vt-kam-allowed",
        "wait-after-command",
        "window-colorspace",
        "window-decoration",
        "window-height",
        "window-inherit-font-size",
        "window-inherit-working-directory",
        "window-new-tab-position",
        "window-padding-balance",
        "window-padding-color",
        "window-padding-x",
        "window-padding-y",
        "window-position-x",
        "window-position-y",
        "window-save-state",
        "window-show-tab-bar",
        "window-step-resize",
        "window-subtitle",
        "window-theme",
        "window-title-font-family",
        "window-titlebar-background",
        "window-titlebar-foreground",
        "window-vsync",
        "window-width",
        "working-directory",
        "x11-instance-name"
      ],
      "overrides": {
        "adjust-box-thickness": {
          "status": "planned",
          "note": "Exact behavior depends on Ghostty's private generated-box sprite and font-classification pipeline, which is not exposed through public libghostty-vt."
        },
        "adjust-cell-height": {
          "status": "supported",
          "note": "Absolute and percentage modifiers apply to the regular-face cell height in rounded physical pixels at the pane's current DPR. The pinned sparse-map application order is preserved, including its dependent baseline and decoration recentering, while cursor height keeps its independent basis."
        },
        "adjust-cell-width": {
          "status": "supported",
          "note": "Absolute and percentage modifiers apply to the regular-face cell advance in rounded physical pixels at the pane's current DPR and drive both rendering geometry and PTY columns."
        },
        "adjust-cursor-height": {
          "status": "supported",
          "note": "The modifier applies in rounded physical pixels to the unadjusted cell-height basis, clamps only to a one-pixel minimum, and vertically centers the result; a configured full cursor may intentionally exceed the cell."
        },
        "adjust-cursor-thickness": {
          "status": "supported",
          "note": "The modifier applies to Ghostty's one-physical-pixel cursor-stroke basis and controls bar, underline, and hollow cursor strokes."
        },
        "adjust-font-baseline": {
          "status": "supported",
          "note": "The signed baseline modifier applies in rounded physical pixels in Ghostty's pinned sparse-map order and updates glyph placement live."
        },
        "adjust-icon-height": {
          "status": "planned",
          "note": "Exact behavior depends on Ghostty's private icon and Nerd Font glyph-classification pipeline, which is not exposed through public libghostty-vt."
        },
        "adjust-overline-position": {
          "status": "supported",
          "note": "The signed position modifier applies in rounded physical pixels in Ghostty's pinned sparse-map order; negative positions remain representable for Ghostty-compatible placement."
        },
        "adjust-overline-thickness": {
          "status": "supported",
          "note": "The modifier applies in rounded physical pixels to the regular-face line thickness and drives rendered overline geometry."
        },
        "adjust-strikethrough-position": {
          "status": "supported",
          "note": "The unsigned position modifier applies in rounded physical pixels in Ghostty's pinned sparse-map order, saturates at zero, and drives rendered strike-through geometry."
        },
        "adjust-strikethrough-thickness": {
          "status": "supported",
          "note": "The modifier applies in rounded physical pixels to the regular-face line thickness and drives rendered strike-through geometry."
        },
        "adjust-underline-position": {
          "status": "supported",
          "note": "The unsigned position modifier applies in rounded physical pixels in Ghostty's pinned sparse-map order, saturates at zero, and drives single, double, curly, dotted, and dashed underline placement."
        },
        "adjust-underline-thickness": {
          "status": "supported",
          "note": "The modifier applies in rounded physical pixels to the regular-face underline thickness and drives every rendered underline style."
        },
        "auto-update": {
          "status": "not_applicable",
          "scope": "macos"
        },
    "auto-update-channel": {
      "status": "not_applicable",
      "scope": "macos"
    },
    "background": {
      "status": "supported",
      "note": "The exact pinned parser supplies the color and the libghostty adapter applies it to new and existing panes."
    },
    "background-blur": {
      "scope": "wayland"
    },
    "bell-audio-path": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The pinned parser finalizes relative paths and preserves required/optional provenance through the strict typed wire. With audio enabled, every pane lazily owns one reusable QMediaPlayer/QAudioOutput pipeline, rebuilds only for a changed identity or invalid medium, retries inaccessible paths on later bells, and keeps optional failures quiet."
    },
    "bell-audio-volume": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The raw finite f64 value is transported without premature normalization, applies live on the next BEL, and is clamped to zero through one only at the QAudioOutput playback boundary."
    },
    "bell-features": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The strict schema transports Ghostty's complete finalized system/audio/attention/title/border set. Every BEL independently calls Qt's best-effort public platform beep for system and replays the pane-local custom source for audio, while also latching its source pane and publishing an every-event GUI notification. Focus, non-modifier key or IME interaction, and any terminal mouse press clear that pane. Title prefixes only the active surface's displayed raw-derived tab/window title, border uses a three-pixel half-opacity input-transparent per-pane overlay, and attention requests QWindow's native indefinite alert only for an inactive host. Inactive tabs retain a separate stable attention marker until selected, including bells from non-active splits. All live changes apply to the next BEL without clearing the latch or changing pane/grid/PTY identity."
    },
    "bold-color": {
      "status": "supported",
      "note": "The renderer implements Ghostty's unset, bright-palette, and fixed-color bold foreground rules while retaining palette/direct/default color provenance."
    },
    "click-repeat-interval": {
      "status": "supported",
      "note": "The strict config projection carries Ghostty's finalized whole-millisecond value, including the Linux 500 ms default. Qt converts a nonzero arbitrary-origin millisecond window-system timestamp to nanoseconds with checked multiplication; zero or overflow produces an untimed press. The worker supplies the live interval and physical cell width, and libghostty accepts a repeat at inclusive consecutive-time and Euclidean-distance thresholds, with distance anchored to the original first press. It owns cell/word/line classification and clamps later repeats at triple; Linux Ctrl maps triple-click to OSC 133 semantic output while Meta/Super remains line. A Shift-left released by the effective mouse-shift-capture policy becomes an immediate drag only when a selection exists and its comparable timestamp is strictly more than the live interval after the retained ordinary press; the exact boundary remains an ordinary repeat candidate. The extension preserves libghostty's tracked anchor and selection behavior without replacing the retained press time, and Linux Ctrl+Alt selects rectangle mode for ordinary drags or delayed Shift extension. The pointer becomes a crosshair for Ctrl+Alt normally or Shift+Ctrl+Alt under raw DEC tracking, below hidden/link cursor priority. Qt ignores MouseButtonDblClick because its physical press already arrived. Reload affects the next press without reshaping an installed selection; ordinary release preserves repeat history, reported physical buttons reset it, and reported motion, wheels, or captured fractional scrolling preserve it."
    },
    "clipboard-paste-bracketed-safe": {
      "status": "supported",
      "note": "The live default-true policy trusts newlines only while Ghostty reports bracketed-paste mode, still rejects an embedded end fence, and shares one mode snapshot with encoding."
    },
    "clipboard-paste-protection": {
      "status": "supported",
      "note": "The live default-true policy is enforced on SessionWorker. Unsafe immutable text is correlated through pane-local request IDs; confirmation re-snapshots mode, and rejected or cancelled requests do not scroll, write, or mark activity."
    },
    "clipboard-trim-trailing-spaces": {
      "status": "supported",
      "note": "The current selection is formatted on the session thread through libghostty's plain formatter with the live trim option and soft-wrap unwrapping."
    },
    "command": {
      "status": "partial",
      "note": "An initial positional command exists, but Ghostty config and initial-command semantics do not."
    },
    "config-file": {
      "status": "partial",
      "note": "The pinned parser loads includes; existing include files and directories for missing optional includes participate in asynchronous live reload, and failed loads retry to discover newly created required includes."
    },
    "confirm-close-surface": {
      "status": "partial",
      "note": "false, true, and always map to live workspace close policy. Linux PTY foreground groups plus a command-submission latch detect external jobs, but long-running shell builtins remain indistinguishable from an idle shell until Ghostty semantic prompt metadata and shell integration are wired."
    },
    "copy-on-select": {
      "status": "partial",
      "note": "Linux false, primary-only true, and clipboard destination modes apply live, including standard fallback when primary selection is unavailable. Automatic copies currently contain plain text instead of Ghostty's mixed plain-plus-HTML payload; the missing exact public formatter contract is tracked in REQUIRES_UPSTREAM.md."
    },
    "cursor-color": {
      "status": "supported",
      "note": "The exact pinned parser supplies explicit colors and cell foreground/background aliases; live default updates preserve terminal OSC 12 overrides and OSC 112 reveals the newest configured default."
    },
    "cursor-opacity": {
      "status": "supported",
      "note": "Configured cursor opacity is clamped and applied to the focused cursor; an unfocused cursor becomes Ghostty's opaque hollow outline."
    },
    "cursor-style": {
      "status": "supported",
      "note": "Block, bar, underline, and hollow-block defaults apply to new and existing panes while terminal DECSCUSR state survives reload until reset."
    },
    "cursor-style-blink": {
      "status": "partial",
      "note": "The configured default is applied, but public libghostty-vt exposes only a boolean setter and cannot preserve Ghostty's explicit true/false precedence over DEC mode 12."
    },
    "cursor-text": {
      "status": "supported",
      "note": "Fixed, cell-foreground, and cell-background cursor text colors are resolved per rendered cursor cell with Ghostty's unset fallback."
    },
    "env": {
      "status": "supported",
      "note": "The private typed projection preserves Ghostty's finalized ordered raw-byte override map, including repeated-key replacement, configured-key removal, whole-map reset, and include precedence. Each pane snapshots the map at construction and applies it byte-exactly after inherited and terminal-injected values; a concrete working directory writes PWD last, while a configured PATH is visible to the child without changing parent-PATH executable lookup. Removal/reset do not unset inherited variables. Embedded NUL is rejected as an execve-safety hardening."
    },
    "faint-opacity": {
      "status": "supported",
      "note": "Configured faint opacity applies to glyphs and text decorations without dimming cell backgrounds."
    },
    "focus-follows-mouse": {
      "status": "supported",
      "note": "The exact default-false boolean reloads live per pane. Accepted hover and button-drag motion focuses an unfocused pane only while its existing Qt window is active, so an inactive host is never raised and retains its current focus item. The shared physical-pixel filter rejects synthetic same-position motion and accumulates subpixel movement against the last accepted position. Focus publication is guarded against synchronous pane or workspace destruction."
    },
    "font-family": {
      "status": "partial",
      "note": "Pinned parsing, exact CLI precedence, four-role propagation, live reload, and ordered Qt fallback families are implemented. Status remains partial because Ghostty's embedded production fallback stack and resolver are not exposed through public libghostty-vt, so Qt's platform resolver supplies the final fallback behavior."
    },
    "font-family-bold": {
      "status": "supported",
      "note": "The finalized ordered family list selects the bold rendering role independently and reloads live; an unavailable styled face falls back to the regular role."
    },
    "font-family-bold-italic": {
      "status": "supported",
      "note": "The finalized ordered family list selects the bold-italic rendering role independently and reloads live; an unavailable styled face falls back to the regular role."
    },
    "font-family-italic": {
      "status": "supported",
      "note": "The finalized ordered family list selects the italic rendering role independently and reloads live; an unavailable styled face falls back to the regular role."
    },
    "font-size": {
      "status": "supported",
      "note": "The pinned f32 value, exact CLI precedence, initial geometry, inheritance, and live reload are implemented. Manually adjusted panes retain their local size while adopting other typography changes; reset_font_size restores the newest live default and resumes following reloads."
    },
    "font-style": {
      "status": "supported",
      "note": "Ghostty's automatic, disabled, and named style alternatives are preserved in the typed wire and applied to the regular rendering role; pinned runtime semantics normalize disabled regular style to automatic."
    },
    "font-style-bold": {
      "status": "supported",
      "note": "Ghostty's automatic, disabled, and named style alternatives select the bold role; disabled or unavailable styled faces fall back to the regular role."
    },
    "font-style-bold-italic": {
      "status": "supported",
      "note": "Ghostty's automatic, disabled, and named style alternatives select the bold-italic role; disabled or unavailable styled faces fall back to the regular role."
    },
    "font-style-italic": {
      "status": "supported",
      "note": "Ghostty's automatic, disabled, and named style alternatives select the italic role; disabled or unavailable styled faces fall back to the regular role."
    },
    "foreground": {
      "status": "supported",
      "note": "The exact pinned parser supplies the color and the libghostty adapter applies it to new and existing panes."
    },
    "fullscreen": {
      "status": "supported",
      "scope": "linux",
      "note": "The exact pinned enum defaults false. True and every non-native variant map to Qt native fullscreen on Linux for each subsequently created window; reload never changes existing window state. When maximize is also true, action- and compositor-driven fullscreen exits restore maximized."
    },
    "initial-window": {
      "status": "supported",
      "scope": "linux",
      "note": "The exact default-true boolean is sampled once per launching process. False starts a primary with no QML root or surface and makes an occupied-name secondary exit successfully without activation; a fresh true launcher still activates that false-started primary. The installed D-Bus service explicitly starts a unique false-initial-window host, then its queued standard activation creates exactly one first surface. Explicit bootstrap CLI values outrank configuration and reload. Local/application/process/desktop creation remains unconditional. The first session that successfully initializes receives the current one-shot program and hold values. Construction or presentation failure before libghostty-vt initialization, terminal initialization failure, and closing a deferred root before start do not consume them; a later child-launch failure does. Later sessions use ordinary launch defaults, and reload alone creates or closes nothing. Following pinned GTK's executable requested-window guard, a process that has never created a surface stays resident; the documented initial-delay behavior is not reachable because GTK exposes no startup quit-timer hook."
    },
    "keybind": {
      "status": "partial",
      "note": "A private versioned export carries Ghostty's finalized root and named-table bindings. Per-surface table stacks, shared-prefix sequences, physical/Unicode/catch-all triggers, chains, local flags, stage-time VT encoding, invalid replay, application-wide all/global dispatch, and Linux XDG portal registration are implemented for the supported action subset. Selection-dependent copy, search, endpoint adjustment, and viewport movement use correlated worker-authoritative performability across separate input events; local and broad chains wait for ordered GUI effects before continuing. Portal registration intentionally matches the pinned GTK frontend's root/direct/single-action restriction."
    },
    "link": {
      "status": "planned",
      "note": "User-defined regular expressions and arbitrary link actions are not implemented because the pinned Ghostty RepeatableLink.parseCLI returns error.NotImplemented. The built-in default matcher controlled by link-url is supported independently, alongside explicit OSC 8 destinations."
    },
    "link-previews": {
      "status": "supported",
      "note": "The exact true/false/osc8 policy previews all, no, or only OSC 8 accepted destinations. Live reload is pane-local and frontend-only: the bounded, middle-elided overlay safely escapes raw destination controls, replaces invalid UTF-8, relocates from bottom-left to bottom-right when entered, and reuses a hover that remains on the terminal link. Removing an occupied guard resumes physical hit testing and may query the newly exposed cell."
    },
    "link-url": {
      "status": "supported",
      "note": "The pinned default URL/path expression and vendored Oniguruma semantics run on complete semantic logical lines. The setting defaults true, reloads live, and controls only regex links; explicit OSC 8 destinations remain interactive when disabled."
    },
    "maximize": {
      "status": "supported",
      "note": "The exact default-false boolean presents every subsequently created window maximized. Initial, action-created, resident-replacement, and desktop-activated windows share the same creation path; reload affects future windows without mutating existing state."
    },
    "middle-click-action": {
      "status": "supported",
      "note": "Linux primary-paste and ignore reload live; primary paste follows Ghostty's copy-on-select-dependent source choice and primary-to-standard fallback while terminal mouse reporting retains precedence."
    },
    "mouse-hide-while-typing": {
      "status": "supported",
      "note": "The exact default-false boolean reloads live per pane. A nonempty, non-repeating text press hides only when it is forwarded to the terminal, including invalid-sequence flush and unavailable performable fallback; consumed bindings, staged leaders, modifiers, releases, raw actions, and paste are excluded. A nonempty IME commit hides while preedit alone does not. Physical motion of at least one device pixel, pointer leave, button press/release, wheel input, focus in/out, or disabling reveals it. Deferred keys and action chains retain their original pointer-activity epoch so late fallback cannot re-hide after newer interaction or become retroactively eligible when the policy is enabled. The pane cursor arbiter gives blank-while-typing priority over a retained hyperlink hand, rectangle crosshair, and raw-state-derived arrow or I-beam."
    },
    "mouse-reporting": {
      "status": "supported",
      "note": "The default-true policy gates terminal-requested DEC mouse tracking per pane without changing raw terminal mode. The conjunction is reevaluated per pointer event. Independently of that frontend gate, ordinary non-overlapping raw DEC state changes the base pointer from an I-beam to an arrow, while Shift temporarily restores the I-beam regardless of mouse-shift-capture policy. Reported physical buttons clear selection and reset repeat-click history, motion does neither, and wheels plus captured fractional scrolling clear selection while preserving that history. Raw DEC capture still governs live mouse-shift-capture modifier normalization; reload replaces local runtime toggles, and future panes use configured state. Arbitrary OSC 22 application shapes and full Ghostty's overlapping-mode transition ordering remain blocked on a public effective-shape query tracked in REQUIRES_UPSTREAM.md."
    },
    "mouse-scroll-multiplier": {
      "status": "supported",
      "note": "The strict config projection preserves Ghostty's independently finalized precision and discrete values. Qt pixel deltas select precision input and fractional angle deltas select discrete input; pane-local signed pending physical distance converts both against current cell height and routes identical whole-row counts through typed viewport scrolling or repeated DEC button 4/5 reports. Captured sub-row input clears selection only after a worker-owned DEC-state recheck. Live reload affects subsequent deltas without replacing the pane or discarding accumulated distance. A GUI dispatch is bounded to 10,000 rows and retains excess movement. Pinned Surface.zig documents retaining post-row remainder but currently clears it by subtracting the untruncated amount; the Qt path intentionally implements the documented accumulator contract so high-resolution motion is not lost."
    },
    "mouse-shift-capture": {
      "status": "partial",
      "note": "The strict config projection transports false, true, always, and never, and reload applies to every existing pane without replacement. Always keeps Shift in captured DEC button input and never releases captured press/release and held-button motion for local selection, middle/right actions, and Ctrl+Shift links; those override-independent capture-routing branches are exact. True and false consistently use their configured capture/release fallback for those same routes, but cannot honor a later XTSHIFTESCAPE request because official libghostty-vt does not expose its internal unset/false/true flag. Matching Ghostty, captured wheel/fractional scrolling and buttonless DEC-motion reporting remain reported regardless of this setting, while buttonless Ctrl+Shift hover still consults it for local hyperlink eligibility. A locally routed delayed Shift-left extends an existing selection through the retained libghostty gesture, including Linux Ctrl+Alt rectangle mode. The raw-DEC-aware Shift I-beam and Shift+Ctrl+Alt crosshair intentionally remain independent of this capture policy. Status remains partial solely because the required public XTSHIFTESCAPE query is tracked in REQUIRES_UPSTREAM.md."
    },
    "palette": {
      "status": "supported",
      "note": "All 256 canonical defaults are applied. Config reload preserves terminal OSC 4 overrides, and OSC 104 resets entries to the newest configured defaults."
    },
    "palette-generate": {
      "status": "planned",
      "note": "Ghostty applies generation later in termio.DerivedConfig using the explicit-entry mask plus foreground and background; the schema-v1 frontend projection carries the finalized base palette but not that derived state."
    },
    "palette-harmonious": {
      "status": "planned",
      "note": "Ghostty applies harmonization later in termio.DerivedConfig; the schema-v1 frontend projection does not carry the derived palette or explicit-entry mask required for exact behavior."
    },
    "quit-after-last-window-closed": {
      "status": "supported",
      "scope": "portable",
      "note": "The application-owned GUI-thread policy uses Ghostty's Linux default, disables Qt's implicit last-window exit, and applies only after a real final registered primary window closes. A never-requested initial-window=false process remains resident, matching pinned GTK's requested-window guard. False leaves the process-level config, shortcuts, reload, quit, any enabled activation endpoint, and new-window factory alive with zero QML roots; either a local action or an acknowledged true bare-secondary launch can register a replacement before presentation. Reload reconciles active state, an explicit command forces true, and explicit quit bypasses the policy."
    },
    "quit-after-last-window-closed-delay": {
      "status": "supported",
      "scope": "linux",
      "note": "The private structured export preserves null versus configured zero and applies Ghostty's exact whole-millisecond truncation and c_uint saturation. One application-owned QChronoTimer starts only after a real final surface closes, is cancelled by a successfully presented replacement, remains armed after factory failure, is bypassed by explicit quit and explicit-command launches, and is reconciled on live reload without moving an identical deadline. Pinned config prose describes a never-windowed initial timeout, but the GTK App lacks the startup timer method; this follows executable GTK behavior while intentionally repairing its separate stale-timer reload edge."
    },
    "resize-overlay": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The exact always/never/after-first mode controls one input-transparent 120x40-logical-pixel QML item per pane. Only accepted authoritative column/row changes schedule it: synchronous bursts coalesce to the newest grid, a later grid restarts the single QChronoTimer, raw pixel and DPR-only changes are inert, and split panes remain independent. After-first suppresses each pane's initial grid and further layout settling during Ghostty's initial 250 ms delay; always includes the first ordinary immediate grid, while pre-worker compositor negotiation is never classified as a resize, and never cancels visible or queued presentation. Reload applies live."
    },
    "resize-overlay-duration": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The private structured export preserves Ghostty's finalized whole-millisecond duration. The pane timer applies the pinned GTK 250 ms minimum and 750 ms default; a duration reload restarts an already visible overlay from the new value."
    },
    "resize-overlay-position": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "All seven exact values—center, top-left, top-center, top-right, bottom-left, bottom-center, and bottom-right—map in logical coordinates relative to the owning pane. Reload moves an existing visible overlay immediately, independently for split panes and at any device scale."
    },
    "right-click-action": {
      "status": "partial",
      "scope": "qt_mapping",
      "note": "All five finalized values reload live and preserve DEC mouse-reporting precedence with the live mouse-shift-capture decision; raw DEC state governs modifier normalization even when mouse-reporting disables effective routing. Paste, copy, copy-or-paste, and ignore are resolved atomically against worker-owned selection state, and independently correlated paste effects are not lost to popup supersession. Context-menu preserves a containing selection or selects an exact-Ctrl OSC 8 cell, full default-regex match, or configured word before requesting one stable-pane-targeted, in-scene Qt Quick Popup.Item menu with Copy, Paste, Reset, a Split submenu containing Change Title, all four directional splits, and Close Split, a Tab submenu containing Change Tab Title, New Tab, and Close Tab, a Window submenu containing New Window and Close Window, and a Config submenu containing Open Configuration and Reload Configuration; its retained window-root click point is mapped into the chrome-adjusted application content item immediately before opening, and stale coordinates cannot reselect current content. A selected fixed action consumes its token once after the menu closes, restores the current active pane's focus, and then revalidates and dispatches to its stable origin; window-local actions retain the originating host while application actions cross the established controller path, and resulting modal prompts retain focus. Status remains partial because copy uses the current plain clipboard representation instead of Ghostty's mixed plain-plus-HTML contract, and the menu still omits Notify on Next Command Finish and Clear."
    },
    "search-background": {
      "status": "supported",
      "note": "Fixed, cell-foreground, and cell-background values apply live to candidate matches; the Ghostty default #FFE082 is retained when unset."
    },
    "search-foreground": {
      "status": "supported",
      "note": "Fixed, cell-foreground, and cell-background values apply live to candidate matches; the Ghostty default black is retained when unset."
    },
    "search-selected-background": {
      "status": "supported",
      "note": "Fixed, cell-foreground, and cell-background values apply live to the navigated match; the Ghostty default #F2A57E is retained when unset."
    },
    "search-selected-foreground": {
      "status": "supported",
      "note": "Fixed, cell-foreground, and cell-background values apply live to the navigated match; the Ghostty default black is retained when unset."
    },
    "selection-background": {
      "status": "supported",
      "note": "Fixed and cell-relative colors are resolved per rendered cell; unset uses the effective terminal foreground."
    },
    "selection-clear-on-copy": {
      "status": "supported",
      "note": "Explicit copy formats and emits the installed selection before optionally clearing it in the same session-thread operation; automatic copy-on-select never clears."
    },
    "selection-clear-on-typing": {
      "status": "supported",
      "note": "Live worker-side policy clears only after encoded non-modifier keys, with physical Escape overriding false. IME commit/preedit lifecycle is atomic; consumed bindings, staged/replayed leaders, raw actions, and paste remain excluded."
    },
    "selection-foreground": {
      "status": "supported",
      "note": "Fixed and cell-relative colors are resolved per rendered cell; unset uses the effective terminal background."
    },
    "selection-word-chars": {
      "status": "supported",
      "note": "The strict config projection carries Ghostty's finalized numeric Unicode-scalar array, including its mandatory U+0000 boundary, without reparsing the source string in Qt. Existing panes replace the value live, and the session worker supplies the newest ASCII or non-BMP boundaries to libghostty's selection-gesture press and drag events; reload alone does not reshape an installed selection."
    },
    "split-divider-color": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The exact pinned parser supplies the optional RGB override; existing and future exact-gap divider handles recolor live, while unset preserves the Qt frontend default without changing geometry, opacity, or focus."
    },
    "split-inherit-working-directory": {
      "status": "partial",
      "note": "The exact boolean defaults true and reloads for future splits only. True uses the explicit source pane's newest accepted local-host-validated OSC 7 directory and otherwise falls back to working-directory; false always uses that fallback. Font inheritance remains independent, nested splits consult the newest workspace base, and stale concrete directories fall back to process cwd while retaining pinned Ghostty's requested logical PWD. Full parity remains partial because the helper's CLI-shaped invocation cannot reproduce GTK's unset desktop-launch home default for the fallback setting."
    },
    "split-preserve-zoom": {
      "status": "supported",
      "note": "The exact canonical no-navigation default clears zoom after a successful goto_split, while navigation atomically transfers the zoomed presentation to the destination for tree-order and spatial navigation. Reload affects subsequent navigation without mutating current state; failed navigation is inert, and direct activation or structural split changes retain their existing unzoom behavior."
    },
    "tab-inherit-working-directory": {
      "status": "partial",
      "note": "The exact default-true boolean reloads for future tabs only. True uses the explicit action-target pane, or the current tab's recorded active pane for an empty-context UI request, and inherits its newest accepted local OSC 7 directory; false or a cleared/unavailable report uses working-directory. Stable broad fanout retains each source independently. The shared unset desktop-launch fallback and lossless non-UTF-8 path transport remain incomplete."
    },
    "term": {
      "status": "supported",
      "note": "Ghostty's finalized non-empty raw bytes supply each pane's initially injected TERM and default empty input to xterm-ghostty. The private schema and direct bytewise envp insertion preserve non-UTF-8 values. Reload affects panes constructed afterward; already constructed panes, including deferred panes without a child yet, retain their snapshot. TERMINFO initially points at ghostty-qt's resolved private database and COLORTERM initially equals truecolor; finalized env entries may replace all three before exec."
    },
    "theme": {
      "status": "partial",
      "note": "Static themes can contribute canonical values for the supported appearance slice through the pinned parser; dynamic light/dark theme selection and switching are not implemented."
    },
    "unfocused-split-fill": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "The nullable fixed RGB fill resolves live to the configured background rather than OSC 11 state. One retained, input-transparent final pane rectangle appears only for actually unfocused surfaces in structurally split tabs, and that pane's active search UI suppresses it across tab, zoom, window, and scene lifecycle changes."
    },
    "unfocused-split-opacity": {
      "status": "supported",
      "scope": "qt_mapping",
      "note": "Ghostty's finalized pane opacity is clamped to [0.15, 1.0] and mapped live to its complementary two-decimal overlay alpha. The GUI-only retained rectangle updates existing and future panes without worker projection, text-row rebuilds, or layout work."
    },
        "gtk-custom-css": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-horizontal-tab-scroll": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-opengl-debug": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-quick-terminal-layer": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-quick-terminal-namespace": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-single-instance": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "The upstream GTK key is not consumed. The private schema-v1 export retains its raw value only as an unused compatibility field; ghostty-qt owns equivalent process arbitration through single-instance in $XDG_CONFIG_HOME/ghostty-qt/config and --single-instance."
        },
        "gtk-tabs-location": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "The upstream GTK key is not consumed. ghostty-qt provides its independently designed tabs-location=top|bottom frontend setting, which moves the retained Qt toolbar and tab strip live."
        },
        "gtk-titlebar": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-titlebar-hide-when-maximized": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-titlebar-style": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-toolbar-style": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "gtk-wide-tabs": {
          "status": "not_applicable",
          "scope": "gtk_only",
          "note": "This GTK-specific setting is not consumed. Where appropriate, ghostty-qt will represent the behavior through independently designed frontend settings rather than GTK spellings."
        },
        "language": {
          "scope": "qt_mapping"
        },
        "linux-cgroup": {
          "status": "supported",
          "scope": "linux",
          "note": "The exact never, always, and single-instance policies create one transient systemd scope per pane before exec. single-instance uses ghostty-qt's fixed startup-resolved primary role rather than the unused GTK configuration key; reload affects only panes constructed afterward."
        },
        "linux-cgroup-hard-fail": {
          "status": "supported",
          "scope": "linux",
          "note": "A false value logs scope setup or membership failures and releases the gated child; true prevents exec and reports a launch failure with exit status 127."
        },
        "linux-cgroup-memory-limit": {
          "status": "supported",
          "scope": "linux",
          "note": "The complete nullable uint64 range is transported exactly and forwarded unchanged as the transient scope's MemoryHigh property."
        },
        "linux-cgroup-processes-limit": {
          "status": "supported",
          "scope": "linux",
          "note": "The complete nullable uint64 range is transported exactly and forwarded unchanged as the transient scope's TasksMax property."
        },
        "macos-applescript": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-auto-secure-input": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-custom-icon": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-dock-drop-behavior": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-hidden": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-icon": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-icon-frame": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-icon-ghost-color": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-icon-screen-color": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-non-native-fullscreen": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-option-as-alt": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-secure-input-indication": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-shortcuts": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-titlebar-proxy-icon": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-titlebar-style": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-window-buttons": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "macos-window-shadow": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "quick-terminal-animation-duration": {
          "scope": "wayland"
        },
        "quick-terminal-autohide": {
          "scope": "wayland"
        },
        "quick-terminal-keyboard-interactivity": {
          "scope": "wayland"
        },
        "quick-terminal-position": {
          "scope": "wayland"
        },
        "quick-terminal-screen": {
          "scope": "wayland"
        },
        "quick-terminal-size": {
          "scope": "wayland"
        },
        "quick-terminal-space-behavior": {
          "scope": "wayland"
        },
    "scrollback-limit": {
      "status": "partial",
      "note": "Ghostty's byte-valued config limit is preserved for pane creation, but libghostty-vt cannot resize the history allocation of an existing pane during live reload."
    },
        "scrollbar": {
          "status": "supported",
          "scope": "qt_mapping",
          "note": "The exact system/never enum applies live to a Qt-styled control overlaid independently on every pane. System appears only for an authoritative active-screen range with scrollback; never removes the affordance without disabling other viewport actions. C++ normalizes the worker's u64 total/offset/length and saturates user movement back to an absolute-row request, avoiding QML integer precision loss and feedback loops. Splits and zoom retain each control and session without reserving grid or PTY geometry."
        },
        "wait-after-command": {
          "status": "partial",
          "note": "The --hold launch flag implements one subset of the behavior."
        },
        "window-position-x": {
          "status": "not_applicable",
          "scope": "x11"
        },
        "window-position-y": {
          "status": "not_applicable",
          "scope": "x11"
        },
        "window-decoration": {
          "status": "partial",
          "scope": "qt_mapping",
          "note": "The pinned helper finalizes auto/client/server/none, including boolean aliases, into a typed per-window policy. None maps exactly to Qt::FramelessWindowHint before first presentation and on live reload; initially decorated auto/client/server windows use ordinary Qt negotiation because public Qt exposes no stable per-window Wayland client-versus-server preference. On an existing Wayland native surface, clearing FramelessWindowHint restores Qt client-side framing rather than renegotiating a prior compositor-side frame. A per-window optional runtime override masks reload until its second toggle clears it, new windows use the latest configuration without inheriting another window's override, and flag changes preserve unrelated hints, client size, visibility/window state, and every pane/session identity."
        },
        "window-height": {
          "status": "supported",
          "scope": "qt_mapping",
          "note": "The finalized u32 cell count participates only when paired with nonzero window-width. Every future window derives hidden normal geometry from its resolved Qt terminal font, inherits the live source/focused screen when available, includes declared QML chrome, applies a screen-bounded 10x4 cell minimum, and clamps best-effort before one activation-aware presentation; reload leaves existing windows unchanged. A normal first pane starts immediately from that viewport. Initially maximized/fullscreen windows keep synchronous pane/controller registration but defer first-session initialization until an exposed compositor-assigned viewport remains stable across two presented frames; inactive first tabs derive current geometry from their logical split tree, pending runtime, read-only, focus, and typed input operations retain controller-owned order until initialization, duplicate readiness events cannot restart the session, and close-before-exposure launches no child or process-derived confirmation. The resulting one-shot geometry initializes libghostty and forkpty before child exec; immediate ioctl probes confirm matching rows, columns, and PTY pixels. Tabs, splits, bare-QML fallback initialization, and normal windows remain immediate and cannot inherit the window seed."
        },
        "window-inherit-font-size": {
          "status": "partial",
          "note": "The exact default-true boolean controls new tabs and windows: true copies only the exact action source's current point size, including manual zoom, while false keeps the newest application font-size with CLI precedence. Composite workspace/PaneId identity distinguishes equal pane IDs in different windows; children remain unadjusted and follow later font-size reloads. Status remains partial because pinned GTK currently ignores the false setting despite the documented contract and embedded frontend honoring it."
        },
        "window-inherit-working-directory": {
          "status": "partial",
          "note": "The exact default-true boolean reloads for future windows. True uses the explicit originating workspace and stable PaneId, or the process controller's focused/last-active pane for application actions, and inherits its newest accepted local OSC 7 directory; stale or zero-window sources safely use the newest application fallback. False always uses that fallback. The shared unset desktop-launch fallback and lossless non-UTF-8 path transport remain incomplete."
        },
        "window-new-tab-position": {
          "status": "supported",
          "note": "The exact current/end enum defaults to current. Current inserts after the tab selected immediately before each creation, or appends with no selection; end appends, and the new tab is selected. Action-source inheritance remains independent of the insertion anchor, while reload affects future tabs only."
        },
        "window-show-tab-bar": {
          "status": "supported",
          "scope": "qt_mapping",
          "note": "The exact always/auto/never enum defaults to auto. Always shows the QML tab strip at every tab count, auto hides it for one tab and shows it at two or more, and never hides it; config reload and one/two-tab transitions update the property live. Acceptance covers workspace state and real QML visibility while the surrounding toolbar remains available."
        },
        "window-width": {
          "status": "supported",
          "scope": "qt_mapping",
          "note": "The finalized u32 cell count participates only when paired with nonzero window-height. Every future window derives hidden normal geometry from its resolved Qt terminal font, inherits the live source/focused screen when available, includes declared QML chrome, applies a screen-bounded 10x4 cell minimum, and clamps best-effort before one activation-aware presentation; reload leaves existing windows unchanged. A normal first pane starts immediately from that viewport. Initially maximized/fullscreen windows keep synchronous pane/controller registration but defer first-session initialization until an exposed compositor-assigned viewport remains stable across two presented frames; inactive first tabs derive current geometry from their logical split tree, pending runtime, read-only, focus, and typed input operations retain controller-owned order until initialization, duplicate readiness events cannot restart the session, and close-before-exposure launches no child or process-derived confirmation. The resulting one-shot geometry initializes libghostty and forkpty before child exec; immediate ioctl probes confirm matching rows, columns, and PTY pixels. Tabs, splits, bare-QML fallback initialization, and normal windows remain immediate and cannot inherit the window seed."
        },
        "working-directory": {
          "status": "partial",
          "note": "The shared-config value supplies initial panes plus new-tab, split, and new-window fallback; explicit home/tilde paths, process-cwd and logical-PWD inherit behavior, symlink-sensitive concrete spelling, explicit Qt CLI precedence, live future-surface updates, composite source identity, and pinned stale-PWD/non-fatal-chdir fallback are tested. The helper always appears CLI-launched, so an unset GTK desktop default resolves to inherit rather than context-dependent home; lossless non-UTF-8 path transport remains incomplete."
        },
        "x11-instance-name": {
          "status": "not_applicable",
          "scope": "x11"
        }
      }
    },
    "keybinding_actions": {
      "source": "src/input/Binding.zig",
      "default": {
        "status": "planned",
        "scope": "portable"
      },
      "entries": [
        "activate_key_table",
        "activate_key_table_once",
        "adjust_selection",
        "check_for_updates",
        "clear_screen",
        "close_all_windows",
        "close_surface",
        "close_tab",
        "close_window",
        "copy_title_to_clipboard",
        "copy_to_clipboard",
        "copy_url_to_clipboard",
        "crash",
        "csi",
        "cursor_key",
        "deactivate_all_key_tables",
        "deactivate_key_table",
        "decrease_font_size",
        "end_key_sequence",
        "end_search",
        "equalize_splits",
        "esc",
        "goto_split",
        "goto_tab",
        "goto_window",
        "ignore",
        "increase_font_size",
        "inspector",
        "jump_to_prompt",
        "last_tab",
        "move_tab",
        "navigate_search",
        "new_split",
        "new_tab",
        "new_window",
        "next_tab",
        "open_config",
        "paste_from_clipboard",
        "paste_from_selection",
        "previous_tab",
        "prompt_surface_title",
        "prompt_tab_title",
        "quit",
        "redo",
        "reload_config",
        "reset",
        "reset_font_size",
        "reset_window_size",
        "resize_split",
        "scroll_page_down",
        "scroll_page_fractional",
        "scroll_page_lines",
        "scroll_page_up",
        "scroll_to_bottom",
        "scroll_to_row",
        "scroll_to_selection",
        "scroll_to_top",
        "search",
        "search_selection",
        "select_all",
        "set_font_size",
        "set_surface_title",
        "set_tab_title",
        "show_gtk_inspector",
        "show_on_screen_keyboard",
        "start_search",
        "text",
        "toggle_background_opacity",
        "toggle_command_palette",
        "toggle_fullscreen",
        "toggle_maximize",
        "toggle_mouse_reporting",
        "toggle_quick_terminal",
        "toggle_readonly",
        "toggle_secure_input",
        "toggle_split_zoom",
        "toggle_tab_overview",
        "toggle_visibility",
        "toggle_window_decorations",
        "toggle_window_float_on_top",
        "unbind",
        "undo",
        "write_screen_file",
        "write_scrollback_file",
        "write_selection_file"
      ],
      "overrides": {
        "activate_key_table": {
          "status": "supported",
          "note": "Decodes Ghostty's canonical action byte escapes and activates the exact named table in the current pane with duplicate-top and maximum-depth rules."
        },
        "activate_key_table_once": {
          "status": "supported",
          "note": "Decodes Ghostty's canonical action byte escapes, activates the exact pane-local one-shot table, and pops it when the innermost table supplies a match."
        },
        "adjust_selection": {
          "status": "supported",
          "note": "Adjusts the logical selection endpoint in every pinned direction and scrolls an endpoint outside the viewport back into view within one session-thread transaction. Absence is worker-authoritatively unperformed; correlated local and broad barriers prevent a stale GUI selection cache from changing performability or action order."
        },
        "check_for_updates": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "clear_screen": {
          "status": "blocked_upstream",
          "note": "Ghostty's semantic clear is not an escape-sequence alias: it is inert on the alternate screen, clears selection and scrollback, distinguishes semantic prompts, clears Kitty graphics when appropriate, and conditionally writes form feed to the child. Official libghostty-vt exposes neither that composite operation nor a sufficient exact primitive set; the required public contract is tracked in REQUIRES_UPSTREAM.md."
        },
        "close_all_windows": {
          "status": "not_applicable",
          "scope": "linux",
          "note": "Deprecated upstream and intentionally a no-op on Linux."
        },
        "close_surface": {
          "status": "partial",
          "note": "Accepts only the exact void spelling and targets the originating stable PaneId plus its containing TabId. Local closure preserves an unrelated active pane, otherwise focuses the previous tree-order leaf except that the leftmost target selects the next leaf; it recursively collapses the split tree, clears only a removed zoom target, removes a final-leaf tab, and approves ordinary shutdown of only that pane's window after the final tab. Pinned GTK also selects the target's neighbor for an inactive programmatic close, whereas Qt deliberately preserves the unrelated active pane. Read-only/running confirmation uses correlated nonzero IDs, revalidates the stable target, and resolves plus commits atomically against synchronous topology observers; window-close approval permanently rejects later structural typed workspace actions so no worker can miss the shutdown sweep, while application-quit escalation and non-structural application or pane-local chain steps remain eligible to finish. Final closure then enters the application-owned last-window policy only if no other primary window remains. Real all:/global: bindings are consumed without PTY input and intentionally converge on one atomic confirmation/window close per Qt workspace. Pinned Ghostty instead fans broad actions to each surface in creation/swap-remove order: GTK closes safe leaves immediately and requests per-surface confirmation for unsafe leaves, while retaining only one pending split-close handle rather than independent stable requests. Full parity also remains limited by the separately tracked prompt-marker approximation."
        },
        "close_tab": {
          "status": "partial",
          "note": "Accepts the exact omitted/this/other/right grammar through a typed mode, anchors to the originating pane's stable containing tab, treats valid no-target modes as performed, correlates dialog responses, and freezes TabId membership across confirmation, reordering, insertion, focus restoration, and overlapping worker shutdown. Commit traverses those frozen members in the then-current reverse visual order. Qt matches Ghostty macOS only in grouping a multi-tab close behind one atomic confirmation: macOS recomputes membership after confirmation and traverses ascending, while Qt retains the original membership. Pinned GTK instead confirms each unsafe target independently and immediately closes safe targets. Broad fanout also follows Qt's tab/tree snapshot order rather than Ghostty's surface creation/swap-remove order, while broad default/this retains the pre-existing single-workspace coalescing."
        },
        "close_window": {
          "status": "supported",
          "note": "The exact void surface action requests ordinary closure of only the originating Qt window, aggregates that window's stable panes behind one confirmation, and starts all of its worker shutdowns concurrently. Non-final closure leaves process lifetime untouched; final closure enters the resident/immediate/delayed application policy. Broad fanout resolves unique registered workspaces so each window receives one close request."
        },
        "copy_to_clipboard": {
          "status": "partial",
          "note": "Unmapped plain copy uses Ghostty's public selection formatter with live trailing-space trimming and worker-atomic optional clearing. Selection presence and formatting are worker-authoritative even across separate input events. Correlated local chains commit the clipboard before continuing, while all/global chains prepare panes concurrently and commit in stable workspace/tab/tree order before advancing. The accepted mixed spelling currently emits plain text, while mapped plain, VT, HTML, and exact mixed formatting require clipboard-specific formatter state not exposed by official libghostty-vt; the required contract is tracked in REQUIRES_UPSTREAM.md."
        },
        "copy_title_to_clipboard": {
          "status": "partial",
          "note": "Local behavior is exact: only the void spelling is accepted, and the originating pane's raw persistent surface override or optional base is copied synchronously and byte-for-QString exact to the standard clipboard. Absent/empty titles return false; tab overrides, zoom/fallback labels, primary, trimming, selection clearing, focus, and terminal selection are excluded. All/global last-writer order follows Qt's tab/tree traversal rather than Ghostty's creation/swap-remove surface vector. Qt also models a direct program name as presentation fallback where pinned Ghostty may initialize it as a copyable base title."
        },
        "copy_url_to_clipboard": {
          "status": "partial",
          "note": "Copies either the hovered OSC 8 destination or pinned default-regex match byte-exact as the text/plain MIME payload without QUrl normalization. Custom link rules/actions remain unavailable; malformed explicit destinations can be copied but are rejected before Qt desktop-service activation."
        },
        "csi": {
          "status": "supported",
          "note": "Restores the configured byte payload from Ghostty's canonical action format, queues it with the CSI introducer as one contiguous sequence on the worker thread, and scrolls the viewport to the active area."
        },
        "decrease_font_size": {
          "status": "supported",
          "note": "Parses the required payload with Ghostty's f32 grammar, clamps the delta to 0..255 (including pinned NaN/infinity behavior), computes max(1, current-delta) in binary32, and marks each targeted pane manually adjusted even for an effective no-op."
        },
        "deactivate_all_key_tables": {
          "status": "supported"
        },
        "deactivate_key_table": {
          "status": "supported"
        },
        "end_key_sequence": {
          "status": "supported",
          "note": "Root and named-table sequences flush only their staged leaders. A normally consumed binding suppresses its terminating key, an explicitly unconsumed binding subsequently encodes that key, broad fanout flushes inactive-pane sequences, and an invocation without an active sequence is a successful no-op."
        },
        "end_search": {
          "status": "partial",
          "note": "Stops the cooperative pane scan, clears its highlights, and hides the pane-local Qt overlay. The public libghostty-vt artifact omits Ghostty's xev-dependent search thread, so this uses the frontend search foundation."
        },
        "equalize_splits": {
          "status": "supported",
          "note": "Equalizes the source tab with Ghostty's same-axis subtree weighting while preserving focus and split zoom."
        },
        "esc": {
          "status": "supported",
          "note": "Restores the configured byte payload from Ghostty's canonical action format, queues it with the ESC introducer as one contiguous sequence on the worker thread, and scrolls the viewport to the active area."
        },
        "goto_split": {
          "status": "supported",
          "note": "Supports wrapped tree-order previous/next traversal, wrapped spatial directions, and legacy top/bottom aliases. Successful navigation consults the live split-preserve-zoom policy: canonical navigation transfers an existing zoom to the destination, while the canonical no-navigation default clears it; failed navigation is inert."
        },
        "goto_tab": {
          "status": "supported",
          "note": "Selects a one-based tab index, rejects zero or values outside Ghostty's c_int frontend range at execution, and clamps higher representable indices to the final tab."
        },
        "goto_window": {
          "status": "supported",
          "note": "Accepts exactly previous/next while retaining Ghostty's surface scope, then relays typed requests to the process-owned window registry. Traversal starts at the active registered root or otherwise tests the first registration, follows Qt registration order with one wrap, skips active, hidden, close-committed, invalid, and active-pane-less roots, restores minimized destinations, requests activation synchronously, and focuses the destination's active pane. Guarded activation and focus support reentrant traversal and callback-scheduled retirement without retaining stale registry objects; final activation remains compositor-mediated on Wayland. All/global invokes the traversal once per visited surface, matching pinned action-major fanout. Pinned GTK traverses its toolkit top-level list, whose ordering is not a portable contract; Qt deliberately uses stable creation/registration order."
        },
        "increase_font_size": {
          "status": "supported",
          "note": "Parses the required payload with Ghostty's f32 grammar, clamps the delta to 0..255 (including pinned NaN/infinity behavior), computes min(current+delta, 255) in binary32, and marks each targeted pane manually adjusted even for an effective no-op."
        },
        "ignore": {
          "status": "supported",
          "note": "The exact app-scoped no-op suppresses press/repeat encoding even on unconsumed bindings, deliberately leaves the corresponding release unsuppressed, wins in the root pre-pass before active named tables, and preserves Ghostty's bare catch-all invalid-sequence drop. All/global executes once and retains the same special release behavior."
        },
        "inspector": {
          "scope": "qt_mapping"
        },
        "jump_to_prompt": {
          "status": "blocked_upstream",
          "note": "Official libghostty-vt does not expose Ghostty's prompt-delta viewport operation. The required public contract and acceptance evidence are tracked in REQUIRES_UPSTREAM.md; ghostty-qt carries neither a Qt scrollback scan nor a local Ghostty patch."
        },
        "last_tab": {
          "status": "supported",
          "note": "Selects the final tab, matching Ghostty's non-MRU semantics."
        },
        "move_tab": {
          "status": "supported",
          "note": "Moves the source surface's tab by a signed cyclic offset while retaining stable tab identity and selection."
        },
        "navigate_search": {
          "status": "partial",
          "note": "Supports exact previous/next grammar, cyclic newest-to-oldest navigation, selected-result highlighting, and off-viewport scrolling. Candidate highlights in older viewports can lag until the bottom-up scan reaches them; terminal-data mutation restarts that scan, and inactive-screen results are not preserved independently."
        },
        "new_split": {
          "status": "supported",
          "note": "Supports left/right/up/down placement plus omitted and explicit auto. Auto uses the originating surface's effective pixel aspect, choosing right only when width is greater and down otherwise; every split focuses the new pane and clears split zoom."
        },
        "new_tab": {
          "status": "supported",
          "note": "Local, QML, and broad triggers create and select a tab through the typed workspace, retain each original source pane for directory/font inheritance, and honor the live current/end insertion policy."
        },
        "new_window": {
          "status": "partial",
          "note": "The exact void application action is routed above all workspaces and runs once for root, all, or global dispatch. Surface-originated requests retain the composite originating workspace and stable PaneId for working-directory/font inheritance; application requests use the focused then last-active live pane, and zero-window requests use current process defaults. After suppressed startup, whichever session successfully initializes first receives the current one-shot program and hold values. Constructing, registering, presenting, or closing a deferred window before session start does not consume them; later sessions use ordinary launch defaults. Each window initializes one workspace before presentation, requires matching visual and QObject ownership, registers lifetime/keybindings/config fanout, checkpoints reentrant destruction, and can recreate a resident process after every old root and worker is retired. Bare-secondary and standard desktop Activate requests consume the same source-less path, including the pinned null-parent cwd/configured-font distinction and exact one-shot activation-token/startup-ID presentation. The +new-window/org.gtk.Actions payload and functional standard ActivateAction remain incomplete."
        },
        "next_tab": {
          "status": "supported",
          "note": "Accepts only the exact void spelling, advances with wraparound from the containing window's currently selected tab even when invoked by an inactive source surface, and reports false when no distinct target exists. All/global applies once per stable pane in each workspace, so the net movement is the pane count modulo the tab count."
        },
        "open_config": {
          "status": "supported",
          "note": "Accepts only the exact void spelling and routes one process-scoped request for local, root, all, or global dispatch, including with zero windows. Linux edit selection examines preferred XDG config.ghostty before legacy config, chooses the first non-empty file or otherwise the first existing empty file, recursively creates the preferred path when neither exists, and uses exclusive creation so contents and concurrent creators are preserved. The selected local-file URL is opened through Qt desktop services; preparation and launch failures are logged without falling through to PTY input. This is the GUI action, not the separately tracked +edit-config CLI contract."
        },
        "paste_from_clipboard": {
          "status": "supported",
          "note": "Accepts only the exact void spelling and reads the standard clipboard on the GUI thread. A missing text MIME representation is not performed, an explicitly present empty text representation is performed without PTY bytes, and non-empty text uses the worker-owned bracketed-paste, unsafe-paste confirmation, read-only, and active-screen safety path. All/global reaches every pane."
        },
        "paste_from_selection": {
          "status": "supported",
          "scope": "linux",
          "note": "Accepts only the exact void spelling and reads the Linux primary selection directly on the GUI thread, without the middle-click standard-clipboard fallback. Text MIME presence, explicit-empty behavior, worker paste safety, read-only filtering, and broad pane fanout match paste_from_clipboard; platforms without primary-selection support report the action unperformed."
        },
        "previous_tab": {
          "status": "supported",
          "note": "Accepts only the exact void spelling, moves with wraparound from the containing window's currently selected tab even when invoked by an inactive source surface, and reports false when no distinct target exists. All/global applies once per stable pane in each workspace, so the net movement is the pane count modulo the tab count."
        },
        "prompt_surface_title": {
          "status": "supported",
          "note": "Accepts only the exact void spelling and targets the originating stable PaneId. The field snapshots the raw non-empty surface override when present, otherwise the optional base title or empty text; tab overrides, zoom decoration, and Qt launch fallbacks are excluded. OK preserves non-empty text exactly as a persistent per-pane override, blank clears only that override, and Cancel is inert. OSC, set_surface_title, and reset retain the hidden base while an override masks it. Surface and tab prompts share one modal FIFO; all/global enqueues once per pane, removed targets are resolved or pruned, and stale completions are inert."
        },
        "prompt_tab_title": {
          "status": "supported",
          "note": "Accepts only the exact void spelling with no colon and targets the source surface's tab by stable identity. Each request snapshots the raw override when present or the current display title otherwise, including the modeled zoom prefix; the focused field places its caret at the end without selecting all. OK preserves text exactly, blank clears the override, and Cancel is inert. Requests share one FIFO with surface-title prompts, with all/global retaining one request per surface and no split-tab deduplication. Source-pane removal is harmless while its tab survives; target deletion resolves or prunes its requests, and stale completions cannot rename another tab."
        },
        "quit": {
          "status": "supported",
          "note": "The exact void action is process-scoped and available with zero windows. It snapshots every live workspace before mutation, lets a safe active window host one confirmation when an inactive pane is running or read-only, leaves every window untouched on cancellation, then starts every workspace's worker shutdown before committing one irreversible process quit. It bypasses disabled/delayed last-window policy, handles an already approved host, rehosts if the dialog workspace disappears, and remains idempotent after commitment."
        },
        "redo": {
          "status": "not_applicable",
          "scope": "macos",
          "note": "Pinned Ghostty implements redo only on macOS; the Linux GTK application explicitly leaves it unimplemented."
        },
        "reload_config": {
          "status": "partial",
          "note": "The process-scoped exact void action requests one debounced asynchronous reload even with zero windows. A successful snapshot updates process keybindings, lifetime, every live workspace, and the defaults consumed by future windows; failure retains the last valid state. Frontend notifications remain absent."
        },
        "reset": {
          "status": "supported",
          "note": "Performs libghostty's full terminal reset on the session thread, resynchronizes cached mouse-input, selection, and working-directory state, preserves the frontend's last published base title because upstream emits no application title update, and publishes a complete replacement frame without writing to the PTY."
        },
        "reset_font_size": {
          "status": "supported",
          "note": "Rejects every parameter, restores the pane's newest configured font size without an action clamp, and clears manual adjustment so later live reloads apply. A fresh split uses its inherited effective size as this target until reload replaces it."
        },
        "reset_window_size": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "resize_split": {
          "status": "supported",
          "note": "Moves the nearest matching-axis ancestor divider by the requested pixel amount, clamps its ratio, and preserves focus and zoom."
        },
        "scroll_page_down": {
          "status": "supported",
          "note": "Scrolls down by exactly the pane's full terminal row count."
        },
        "scroll_page_fractional": {
          "status": "supported",
          "note": "Uses the pane's full row count with f32 multiplication and truncation toward zero; non-finite or unsafe values are rejected instead of reproducing the pinned frontend's conversion crash."
        },
        "scroll_page_lines": {
          "status": "supported",
          "note": "Scrolls by the signed i16 line count with Ghostty's boundary clamping."
        },
        "scroll_page_up": {
          "status": "supported",
          "note": "Scrolls up by exactly the pane's full terminal row count."
        },
        "scroll_to_bottom": {
          "status": "supported",
          "note": "Pins the viewport to the active area through libghostty's tagged viewport API."
        },
        "scroll_to_row": {
          "status": "supported",
          "note": "Scrolls to a usize absolute row in libghostty's full-screen/scrollbar coordinate space with terminal-side clamping."
        },
        "scroll_to_selection": {
          "status": "supported",
          "note": "Normalizes the current selection to its visual top-left row and aligns that row to the viewport; absence remains non-performable. The session worker decides that state at execution and returns a correlated result, so local performable keys and process-wide chains do not consult an asynchronously reconciled GUI cache."
        },
        "scroll_to_top": {
          "status": "supported",
          "note": "Pins the viewport to the top of scrollback through libghostty's tagged viewport API."
        },
        "search": {
          "status": "partial",
          "note": "Replaces the literal needle without changing search-UI visibility, and an empty needle stops matching while leaving the overlay alone. The generation-cancelled bottom-up scan uses public grid references, whose reads temporarily restore compressed pages and therefore interleave bounded recompression. Flat public rows cannot exactly reproduce internal page-boundary delimiters or every blank-cell point mapping; mutations restart the scan, older viewport highlights can lag, and only the active screen has results."
        },
        "search_selection": {
          "status": "partial",
          "note": "When a terminal selection exists, its exact untrimmed text becomes the needle and opens the pane overlay; a valid empty selection opens and focuses while retaining the previous entry text, matching pinned GTK sequencing. Without a selection the worker-authoritative correlated action is not performed. Local and broad chains commit the ordered overlay effect before continuing; search execution uses the cooperative frontend foundation."
        },
        "select_all": {
          "status": "supported",
          "note": "Installs Ghostty's full selectable-content range and prepares the configured automatic clipboard copy from the same session-thread transaction. Its correlated result suspends local chains and process-wide action-major fanout until the GUI clipboard effect commits; broad effects publish in stable workspace/tab/tree order. The remaining exact mixed plain-plus-HTML automatic-copy payload limitation is tracked by copy_to_clipboard and REQUIRES_UPSTREAM.md."
        },
        "set_font_size": {
          "status": "supported",
          "note": "Parses the required payload directly to f32, applies Ghostty's 1..255 clamp including NaN and signed-infinity behavior, and marks every targeted pane manually adjusted even when its effective size is unchanged."
        },
        "set_surface_title": {
          "status": "supported",
          "note": "Requires a colon, decodes Ghostty's canonical escaped-byte transport as valid UTF-8, and replaces the originating stable pane's base title without changing focus or selection. An empty payload remains explicitly present rather than selecting the Qt fallback; later OSC metadata replaces it, including restoration of the exact pre-action cached string. Reset preserves the current base title regardless of its latest writer because pinned Ghostty publishes no application title update. Per-tab overrides continue masking base updates, and all/global applies once to every pane."
        },
        "set_tab_title": {
          "status": "supported",
          "note": "Decodes Ghostty's canonical byte-string escapes and valid UTF-8, then applies a non-empty override to the source surface's tab by stable identity. An empty payload clears the override and reveals the active pane's effective surface title; focus and surface/base-title changes plus tab insertion and reordering preserve the override."
        },
        "show_gtk_inspector": {
          "status": "not_applicable",
          "scope": "gtk_only"
        },
        "show_on_screen_keyboard": {
          "scope": "qt_mapping"
        },
        "start_search": {
          "status": "partial",
          "note": "Opens and focuses a pane-local top-right Qt overlay, retaining and selecting its prior text without starting work for an empty needle. The overlay is not draggable and the upstream xev-dependent search thread is unavailable in libghostty-vt."
        },
        "text": {
          "status": "supported",
          "note": "Decodes the pinned Zig string-literal escape grammar on the session thread, preserves Unicode and embedded NUL bytes, writes without key encoding, and consumes malformed literals without PTY output."
        },
        "toggle_background_opacity": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "toggle_command_palette": {
          "scope": "qt_mapping"
        },
        "toggle_fullscreen": {
          "status": "supported",
          "note": "Routes the finalized surface action through the typed workspace pipeline to toggle the containing Qt window; broad all/global fanout applies one transition per workspace, and Ghostty's Linux Ctrl+physical Enter default is active."
        },
        "toggle_maximize": {
          "status": "supported",
          "note": "Routes the exact void surface action to maximize or restore the containing Qt window. Broad all/global fanout is intentionally coalesced once per workspace instead of reproducing pinned GTK's cancel-prone per-surface toggles; separate windows transition independently. Prior visibility, including maximized or minimized state, survives fullscreen round trips, and toggling maximize while fullscreen changes the eventual restore state without leaving fullscreen. Ghostty provides no default binding."
        },
        "toggle_mouse_reporting": {
          "status": "supported",
          "note": "Flips the source pane's mouse-reporting policy without mutating terminal DEC mode. Effective capture is the per-event conjunction of both states; all/global toggles every stable pane once, each pane remains independent, and live reload restores configuration."
        },
        "toggle_readonly": {
          "status": "supported",
          "note": "Toggles read-only state per source surface, including stable all/global fanout. Worker-ordered policy suppresses surface-originated key, IME, mouse-report, paste, and raw-action bytes while output, terminal-generated protocol replies, focus bookkeeping, and terminal-local selection, copy, search, and native scrolling remain active; toggling off resumes input without replay. An input-transparent top-right Read-only badge and active-pane model role publish the state. Read-only forces pane, tab, and workspace close confirmation regardless of confirm-close-surface, foreground activity, or child-exit state."
        },
        "toggle_quick_terminal": {
          "scope": "wayland"
        },
        "toggle_secure_input": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "toggle_split_zoom": {
          "status": "supported",
          "note": "Zooms the active leaf without rebuilding the tree or PTY sessions and publishes the zoom state to the tab strip."
        },
        "toggle_tab_overview": {
          "scope": "qt_mapping"
        },
        "toggle_visibility": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "toggle_window_decorations": {
          "status": "partial",
          "scope": "linux",
          "note": "Accepts only the exact void spelling and requires a source surface attached to a host window. The first invocation installs the opposite none-versus-decorated override from the current configuration; the second clears that override and reveals the latest reloaded value. Local targets change only their containing window, while all/global fanout coalesces exactly once per workspace so multiple panes cannot cancel the toggle. The Qt projection mutates only FramelessWindowHint and leaves window state, geometry, unrelated flags, panes, and terminal workers intact. On Wayland, clearing the hint on an existing native surface restores Qt client-side framing instead of renegotiating prior compositor-side decoration; exact restoration requires native-surface recreation or private Qt APIs."
        },
        "toggle_window_float_on_top": {
          "status": "not_applicable",
          "scope": "macos"
        },
        "unbind": {
          "status": "supported",
          "note": "Implemented at configuration finalization by the pinned Ghostty helper: it removes the target binding before the structured trie reaches Qt. The directive is intentionally absent from the runtime action catalog and cannot execute or appear in a finalized action chain."
        },
        "undo": {
          "status": "not_applicable",
          "scope": "macos",
          "note": "Pinned Ghostty implements undo only on macOS; the Linux GTK application explicitly leaves it unimplemented."
        },
        "write_screen_file": {
          "status": "partial",
          "note": "Accepts the required copy, paste, or open disposition with omitted or explicit plain format and consumes valid no-data invocations. It formats the complete active PageList with soft wraps removed, trailing spaces retained, and trailing blank rows dropped: primary includes history plus screen, while alternate contains only its own page. The persistent screen.txt artifact is mode 0600 inside a deliberately stricter mode-0700 temporary directory; pinned Ghostty leaves the directory at its umask-derived default. Copy publishes the absolute path as standard-clipboard text, paste writes its raw filesystem bytes without newline, quoting, or bracketed-paste framing, and open uses the desktop local-file handler. Correlated completion suspends a local chain until its GUI effect commits; all/global actions prepare every pane concurrently, then commit in stable workspace/tab/tree order before the next entry. Styled VT and HTML formats are recognized but remain unsupported: libghostty-vt's public terminal formatter can emit styled content, but its C options do not expose the exact effective foreground, background, and palette path used by the pinned ScreenFormatter action, so byte-compatible parity still needs a bridge or an explicitly validated equivalent."
        },
        "write_scrollback_file": {
          "status": "partial",
          "note": "Implements the same correlated plain copy, paste, and open pipeline as write_screen_file but formats only primary-screen history into history.txt; alternate-screen use and an empty history are performed no-ops with no artifact. Successful files persist as mode 0600 under a mode-0700 directory instead of pinned Ghostty's umask-derived directory mode. Styled VT and HTML output remains unsupported until the public terminal formatter is bridged to or validated against the pinned ScreenFormatter color options."
        },
        "write_selection_file": {
          "status": "partial",
          "note": "Implements the same correlated plain copy, paste, and open pipeline as write_screen_file for the current active selection, preserving inclusive endpoints and rectangular shape in selection.txt; no selection is a performed no-op with no artifact. Successful files persist as mode 0600 under a mode-0700 directory instead of pinned Ghostty's umask-derived directory mode. Styled VT and HTML output remains unsupported until the public terminal formatter is bridged to or validated against the pinned ScreenFormatter color options."
        }
      }
    },
    "cli_actions": {
      "source": "src/cli/ghostty.zig",
      "default": {
        "status": "planned",
        "scope": "portable"
      },
      "entries": [
        "boo",
        "crash-report",
        "edit-config",
        "explain-config",
        "help",
        "list-actions",
        "list-colors",
        "list-fonts",
        "list-keybinds",
        "list-themes",
        "new-window",
        "show-config",
        "show-face",
        "ssh",
        "ssh-cache",
        "toggle-quick-terminal",
        "validate-config",
        "version"
      ],
      "overrides": {
        "edit-config": {
          "status": "supported",
          "note": "Delegates the pinned Linux implementation before Qt starts: standard config loading and preferred-file creation, non-empty VISUAL then EDITOR selection, preferred/non-empty legacy path choice, shell-escaped path construction, /bin/sh replacement, raw streams, and editor exit status are preserved. The pinned implementation currently leaves a newly created file empty despite its template-writing intent. This is distinct from the GUI open_config action."
        },
        "explain-config": {
          "status": "supported",
          "note": "Delegates the pinned implementation through the relocatable embedded-runtime helper before Qt starts."
        },
        "help": {
          "status": "supported",
          "note": "The pinned +help implementation is delegated; the frontend's ordinary --help remains separate, and the printed upstream catalog is not a support claim for every listed action."
        },
        "list-actions": {
          "status": "supported",
          "note": "Delegates the pinned action catalog through the relocatable embedded-runtime helper before Qt starts."
        },
        "list-colors": {
          "status": "supported",
          "note": "Delegates the pinned color catalog and its action-specific options before Qt starts."
        },
        "list-keybinds": {
          "status": "supported",
          "note": "Delegates the pinned finalized keybinding formatter through the embedded-runtime helper."
        },
        "new-window": {
          "scope": "linux",
          "note": "Ghostty's payload-capable +new-window/org.gtk.Actions protocol remains planned; ordinary bare-secondary and standard desktop source-less activation are tracked separately under the Qt-owned frontend single-instance setting and the new_window application action."
        },
        "show-config": {
          "status": "supported",
          "note": "Delegates the pinned implementation through ghostty-qt's app-runtime=none helper; GTK-runtime-specific finalization is intentionally outside this embedded helper contract."
        },
        "ssh": {
          "status": "supported",
          "note": "Delegates the pinned +ssh wrapper through the relocatable embedded-runtime helper before Qt starts. Ghostty owns wrapper-option parsing, destination resolution, exact wrapped-SSH arguments and streams, TERM and SendEnv forwarding, encoded xterm-ghostty installation and fallback, standard XDG-state cache integration, and child exit or signal-status mapping. Tests cover the action boundary, binary streams, environment and cwd inheritance, arbitrary exit and signal status, install failure, successful upload, cache write, and cached install suppression without network access. This supports explicit +ssh invocation; automatic shell-function injection remains separately tracked by shell-integration and shell-integration-features."
        },
        "ssh-cache": {
          "status": "supported",
          "note": "Delegates the pinned +ssh-cache manager through the relocatable embedded-runtime helper before Qt starts. Ghostty owns the standard XDG-state path, list, exact and bare-host query, add/update, remove, prune, and clear grammar, validation, 0600 repair, atomic on-disk updates, streams, and exit status. Tests use an isolated repository-local state directory; no Qt cache namespace or frontend reimplementation is introduced."
        },
        "toggle-quick-terminal": {
          "scope": "wayland"
        },
        "validate-config": {
          "status": "supported",
          "note": "Delegates the pinned validation implementation with raw arguments, streams, and exit status preserved."
        },
        "version": {
          "status": "partial",
          "note": "--version exists; Ghostty +version details do not."
        }
      }
    }
  }
}
